{
  "name": "batik-klasifikasi-monorepo",
  "description": "Fullstack Batik Classification App (Frontend + Backend + ML Service)",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-backend": "cd backend && npm install",
    "start": "node backend/src/index.js",
    "dev": "cd backend && npm run dev",
    "build": "bash .render-build.sh"
  },
  "dependencies": {},
  "devDependencies": {},
  "note": "Backend dependencies are managed in backend/package.json. Render uses .render-build.sh to install from backend folder."
}
