{
  "name": "batik-klasifikasi-monorepo",
  "description": "Fullstack Batik Classification App (Frontend + Backend + ML Service)",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install-backend": "cd backend && npm install",
    "postinstall": "npm run install-backend",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "build": "npm run install-backend"
  },
  "dependencies": {},
  "devDependencies": {},
  "note": "Backend dependencies are managed in backend/package.json. Render uses the root build/start scripts to install and start from the backend folder."
}
